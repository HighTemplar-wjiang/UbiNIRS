<resources>

    <!-- Base application theme. -->
    <style name="AppCompatStyle" parent="Theme.AppCompat.Light">
        <item name="android:colorAccent">@color/nir_red</item>
        <item name="android:colorFocusedHighlight">@color/nir_red</item>
        <item name="android:colorControlActivated">@color/nir_red</item>
        <item name="android:colorControlNormal">@color/nir_red</item>
        <item name="android:navigationBarColor">@color/nir_red</item>
        <item name="android:color">@color/nir_red</item>
        <item name="android:colorPrimary">@color/nir_red</item>
    </style>

</resources>
